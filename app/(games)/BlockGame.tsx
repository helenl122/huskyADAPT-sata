import {Text, View} from "react-native";
import GameHeader from "@/components/GameHeader"

const BlockGame = () => {
    return (
        <View>
            <GameHeader/>
            <Text>Block Game</Text>
        </View>
    );
}

export default BlockGame;